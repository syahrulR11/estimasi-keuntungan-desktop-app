name: Build JavaFX App

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    name: Build .deb on Linux

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 21
          # Tambahkan caching Gradle untuk mempercepat build
          cache: 'gradle' 

      - name: Grant permission for Gradle wrapper
        run: chmod +x ./gradlew

      - name: Build .deb package
        # Cukup panggil task jpackage. Plugin Gradle akan mengunduh JavaFX secara otomatis.
        # Variabel lingkungan dan skrip download manual tidak diperlukan lagi.
        run: ./gradlew :app:jpackage --stacktrace

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-deb
          path: app/build/jpackage/*.deb

  build-windows:
    runs-on: windows-latest
    name: Build .exe on Windows

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 21
          # Tambahkan caching Gradle untuk mempercepat build
          cache: 'gradle'

      - name: Build .exe package
        # Cukup panggil task jpackage. Plugin Gradle akan mengunduh JavaFX secara otomatis.
        # Skrip download manual tidak diperlukan lagi.
        run: ./gradlew.bat :app:jpackage --stacktrace

      - name: Upload .exe artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: app\build\jpackage\*.exe